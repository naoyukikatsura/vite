// import { useCallback } from "react";

// import {tasks} from './handle-create-task'

// const useHandleValueEdit = () => {
//   const handleValueEdit = useCallback(
//     (id: number, inputValue: string) => {
//       const newTasks = tasks.map((task) => {
//         if (task.id === id) {
//           task.value = inputValue;
//         }

//         return task;
//       });
//       setTasks(newTasks);
//     },
//     [setTasks, tasks]
//   );
// }
